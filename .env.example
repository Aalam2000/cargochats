# === APP ===
ENV=dev
APP_NAME=cargochats
LOG_LEVEL=INFO

# Protect API access (clients send header X-API-Key)
API_KEY=CHANGE_ME_DEV_KEY

# === DB ===
DB_HOST=postgres
DB_PORT=5432
DB_NAME=cargochats
DB_USER=cargochats
DB_PASSWORD=cargochats

# === OpenAI / LLM (placeholder for later) ===
OPENAI_API_KEY=
